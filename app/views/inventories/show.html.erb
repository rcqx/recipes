<%= @inventory.name %><br><br>
<%= link_to 'Add Food', new_food_path, data: {turbo: false} %>

<table>
  <tr>
    <th>Food</th>
    <th>Quantity</th>
    <th>Actions</th>
  </tr>
  <% @inventory.inventory_food.each do |item| %>
    <tr>
      <td><%= item.food.name %></td>
      <td><%= item.quantity %><%= item.food.measurement_unit %></td>
      <td><%= button_to 'Remove', inventory_path(item.id), method: :delete %></td>
    </tr>
  <% end %>
</table>


